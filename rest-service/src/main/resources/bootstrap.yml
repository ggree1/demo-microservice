server:
  port: ${PORT:8083}

spring:
  application:
    name: rest-service

  cloud:
    config:
      uri: http://localhost:8888
      name: rest-service

  h2:
    console:
      enabled: true
      path: /h2-console


datasource:
  url: jdbc:h2:mem:testdb

feign:
  hystrix:
    enabled: true